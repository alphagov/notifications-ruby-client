require 'spec_helper'

describe Notifications::Ruby::Client do
  it 'has a version number' do
    expect(Notifications::Ruby::Client::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
